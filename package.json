{
  "name": "houseapp-monorepo",
  "version": "2.3.2",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "packages/finance-core"
  ],
  "scripts": {
    "test": "npm test --prefix api && npm test --prefix packages/finance-core && node chrome-extension/lib/notification-utils.test.js",
    "precommit": "lint-staged",
    "prepare": "husky install",
    "dev": "concurrently -n api,web -c blue,green \"cd api && TZ=UTC npx tsx watch --env-file ./.env src/http/server.ts\" \"cd web && yarn dev\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "concurrently": "^9.2.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "**/*.{js,ts,tsx,jsx,json}": "biome lint --write --no-errors-on-unmatched --files-ignore-unknown=true"
  }
}
