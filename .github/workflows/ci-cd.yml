name: ci-cd

on:
  push:
    branches: [develop, main]
    paths:
      - 'api/**'
      - 'web/**'
      - 'deploy/stacks/**'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., 1.0.1)'
        required: false
        default: ''
  release:
    types: [published]

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: [self-hosted, linux, ARM64]
    strategy:
      matrix:
        app: [api, web]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:latest

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate semantic version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            # Use release tag for releases
            VERSION="${{ github.event.release.tag_name }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "is_release=true" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.version }}" ]; then
            # Use manual version from workflow_dispatch
            VERSION="${{ github.event.inputs.version }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "is_release=true" >> $GITHUB_OUTPUT
          else
            # Generate semantic version for branches
            BRANCH="${{ github.ref_name }}"
            SHA="${GITHUB_SHA::7}"
            DATE=$(date +%Y%m%d)
            
            if [ "$BRANCH" = "main" ]; then
              VERSION="1.0.0-$DATE-$SHA"
            else
              VERSION="0.1.0-$DATE-$SHA"
            fi
            
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "is_release=false" >> $GITHUB_OUTPUT
          fi
          echo "Generated version: $VERSION"

      - name: Build and push image (native ARM64)
        run: |
          set -e
          REPO_LC=${GITHUB_REPOSITORY,,}
          IMAGE=ghcr.io/${REPO_LC}-${{ matrix.app }}
          VERSION="${{ steps.version.outputs.version }}"
          SHA=${GITHUB_SHA::7}
          BRANCH=${GITHUB_REF_NAME}
          
          echo "Building $IMAGE for native ARM64"
          echo "Tags: $VERSION, $SHA, $BRANCH"
          
          docker build \
            -f ${{ matrix.app }}/Dockerfile \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            -t $IMAGE:$VERSION \
            -t $IMAGE:$SHA \
            -t $IMAGE:$BRANCH \
            ${{ matrix.app }}
          
          docker push $IMAGE:$VERSION
          docker push $IMAGE:$SHA
          docker push $IMAGE:$BRANCH

      - name: Build summary
        run: |
          printf "### Build & Push (%s)\n- Image: ghcr.io/%s-%s\n- Tags: %s, %s, %s\n" \
            "${{ matrix.app }}" "${GITHUB_REPOSITORY,,}" "${{ matrix.app }}" "${{ steps.version.outputs.version }}" "${GITHUB_SHA::7}" "${GITHUB_REF_NAME}" >> "$GITHUB_STEP_SUMMARY"

  deploy:
    needs: build-and-push
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}
    steps:
      - uses: actions/checkout@v4

      - name: Generate semantic version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
          else
            BRANCH="${{ github.ref_name }}"
            SHA="${GITHUB_SHA::7}"
            DATE=$(date +%Y%m%d)
            
            if [ "$BRANCH" = "main" ]; then
              VERSION="1.0.0-$DATE-$SHA"
            else
              VERSION="0.1.0-$DATE-$SHA"
            fi
            
            echo "version=$VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Check deploy secrets
        id: check
        env:
          HAS_HOST: ${{ secrets.VPS_HOST }}
          HAS_USER: ${{ secrets.VPS_USER }}
          HAS_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          missing=0
          [ -z "$HAS_HOST" ] && echo "::warning title=Missing secret::VPS_HOST not set" && missing=1
          [ -z "$HAS_USER" ] && echo "::warning title=Missing secret::VPS_USER not set" && missing=1
          [ -z "$HAS_KEY" ] && echo "::warning title=Missing secret::VPS_SSH_KEY not set" && missing=1
          if [ $missing -eq 1 ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            printf "### Deploy skipped\nMissing secrets: VPS_HOST, VPS_USER and/or VPS_SSH_KEY\n" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Copy files to VPS
        if: steps.check.outputs.skip == 'false'
        run: |
          # Create SSH directory and key file
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key
          
          # Copy stack file to VPS
          scp -i ~/.ssh/vps_key -o StrictHostKeyChecking=no deploy/stacks/houseapp.yml ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/tmp/stack.yml
          
          # Clean up SSH key
          rm ~/.ssh/vps_key

      - name: Deploy over SSH to Swarm
        if: steps.check.outputs.skip == 'false'
        id: deploy
        continue-on-error: true
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            SHA=${GITHUB_SHA::7}
            VERSION="${{ steps.version.outputs.version }}"
            if [ "${GITHUB_REF_NAME}" = "develop" ]; then
              export TAG=develop
            else
              export TAG="$VERSION"
            fi

            # Create directory structure (if not exists)
            sudo mkdir -p /opt/stacks/houseapp

            # Move stack file from /tmp to final location
            sudo mv /tmp/stack.yml /opt/stacks/houseapp/stack.yml

            # Stack.yml is always updated (copied from repository)
            echo "📄 Updating stack.yml..."
            
            # Check if env files exist, if not copy from examples
            
            if [ ! -f /opt/stacks/houseapp/api.env ]; then
              echo "📄 api.env not found, copying from example..."
              # Copy api.env.example from repository to VPS
              curl -s https://raw.githubusercontent.com/${{ github.repository }}/${{ github.ref_name }}/deploy/env/api.env.example | sudo tee /opt/stacks/houseapp/api.env.example > /dev/null
              sudo cp /opt/stacks/houseapp/api.env.example /opt/stacks/houseapp/api.env
            else
              echo "✅ api.env already exists"
            fi
            
            if [ ! -f /opt/stacks/houseapp/web.env ]; then
              echo "📄 web.env not found, copying from example..."
              # Copy web.env.example from repository to VPS
              curl -s https://raw.githubusercontent.com/${{ github.repository }}/${{ github.ref_name }}/deploy/env/web.env.example | sudo tee /opt/stacks/houseapp/web.env.example > /dev/null
              sudo cp /opt/stacks/houseapp/web.env.example /opt/stacks/houseapp/web.env
            else
              echo "✅ web.env already exists"
            fi

            # Final check - if env files are still missing, exit
            if [ ! -f /opt/stacks/houseapp/api.env ] || [ ! -f /opt/stacks/houseapp/web.env ]; then
              echo "❌ Missing required env files:"
              [ ! -f /opt/stacks/houseapp/api.env ] && echo "  - api.env"
              [ ! -f /opt/stacks/houseapp/web.env ] && echo "  - web.env"
              echo "📁 Stack files location: /opt/stacks/houseapp/"
              exit 1
            fi

            echo "✅ All required files found!"
            echo "📁 Stack files location: /opt/stacks/houseapp/"
            echo "📄 Files: stack.yml, api.env, web.env"

            # Login to GHCR and deploy
            docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
            echo "🚀 Deploying houseapp with TAG=$TAG (version: $VERSION)"
            docker stack deploy --with-registry-auth --resolve-image changed \
              -c /opt/stacks/houseapp/stack.yml houseapp
            
            echo "✅ Stack deployed successfully!"
            echo "📊 Check status: docker stack ps houseapp"

      - name: Deploy summary
        if: steps.deploy.outcome == 'success'
        run: |
          printf "### Deploy\n- Branch: %s\n- Version: %s\n- TAG: %s\n- Target: Swarm stack houseapp (SSH)\n" \
            "${GITHUB_REF_NAME}" "${{ steps.version.outputs.version }}" "${GITHUB_SHA::7}" >> "$GITHUB_STEP_SUMMARY"

      - name: Deploy failed - missing files
        if: steps.deploy.outcome == 'failure'
        run: |
          printf "### Deploy Failed\nMissing required env files in /opt/stacks/houseapp/:\n- api.env\n- web.env\n\nPlease ensure env files exist before deploying.\n" >> "$GITHUB_STEP_SUMMARY"
